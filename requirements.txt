# Jarvis voice dictation — direct dependencies (pinned to known-good versions).
# Transitive deps (onnxruntime, ctranslate2, av, tokenizers, ...) resolve automatically.
faster-whisper==1.2.1
openwakeword==0.6.0
sounddevice==0.5.5
pynput==1.8.2
numpy==2.5.0
