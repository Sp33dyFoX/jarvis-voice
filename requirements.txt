# Jarvis voice dictation — direct (top-level) dependencies, pinned.
# For a fully reproducible install, setup uses requirements.lock.txt, which pins
# EVERY package (including transitive deps) to exact versions. This file is the
# human-readable summary of what the project directly depends on.
faster-whisper==1.2.1
openwakeword==0.6.0
sounddevice==0.5.5
pynput==1.8.2
numpy==2.5.0
